# OS cruft
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp

# Local agent tooling state
.serena/
.claude/settings.local.json

# Python
__pycache__/
*.pyc
.pytest_cache/

# pairwork needs no virtualenv (stdlib only), but do not commit one if a
# contributor makes one anyway.
.venv/

# Run records live in ~/.dbhq/pairwork/ and must never be inside a repo - a
# brief can quote an unreleased document and the output quotes it back.
runs.jsonl
runs/
.env
